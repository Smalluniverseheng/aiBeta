# 会员计划方案 — 第三方科技 · AI 智能聚合平台

> 本文档记录会员体系的完整设计方案，供后续 AI 接手时参考。
> 最后更新: 2026-07-28
> 版本: v5.7 规划

---

## 一、核心定位

**本平台是 API 聚合工具，用户自带 API Key。**
会员售卖的是 **存储空间 + 云端同步 + 设备管理 + Worker 代理服务**，而非 AI 对话额度。

---

## 二、会员等级（宇宙主题）

| 等级 | 图标 | 颜色 | 月付 | 年付 | 存储空间 | 设备数 | 云端同步 | Worker 代理 |
|------|------|------|------|------|----------|--------|----------|-------------|
| **游客** 🌑 | 无 | — | ¥0 | ¥0 | 浏览器本地 | 1 | ❌ | ❌ |
| **卫星** 🛰️ | 小卫星 | 灰色 | ¥0 | ¥0 | **10MB** | 1 | ❌ | ❌ |
| **行星** 🪐 | 小星球 | 绿色 | **¥9.9** | ¥99 | **1GB** | **10** | ✅ | ✅ |
| **恒星** ☀️ | 太阳 | 蓝色 | ¥29.9 | ¥299 | **10GB** | **20** | ✅ | ✅ |
| **星系** 🌌 | 螺旋星系 | 紫色 | ¥59.9 | ¥599 | **50GB** | **40** | ✅ | ✅ |
| **宇宙** 🌠 | 大爆炸 | 金色 | ¥99 | ¥990 | **100GB** | **无限** | ✅ | ✅ |

> 年付策略：买10送2（约83折）

---

## 三、权益详解

### 3.1 书源/图源
- **所有等级**（含游客）均可无限导入书源/图源
- 书源不占多少空间，不做限制
- 免费版书源只存本地，换设备丢失
- 付费版书源自动云端同步

### 3.2 存储空间（核心付费指标）

**计入存储的项目：**
- 头像图片（50-200KB）
- 聊天中的图片（100KB-5MB/张）
- 聊天中的文档（10KB-10MB/个）
- 语音消息（50KB-2MB/条）
- 书源 JSON（1-10KB/个）
- 书架数据（1KB/本）
- 历史对话文本（500B-2KB/条）
- 自定义角色（1-5KB/个）
- 小说章节缓存（5-50KB/章）
- 漫画图片缓存（100KB-2MB/张）
- 界面设置（<1KB）

**图片策略：**
- 默认**原图上传**，不压缩
- 设置中添加选项："自动压缩（节省空间）"
- 压缩档位：高质量80% / 中质量60% / 低质量40%
- 用户自行选择，容量有限自行负责

### 3.3 云端同步
- **游客/卫星**：无云端同步，数据纯本地
- **行星及以上**：数据自动备份到 Supabase
- 清除浏览器/换设备后，重新登录自动从云端恢复

### 3.4 Worker 代理
- **游客/卫星**：直接请求书源链接，可能受 CORS 限制
- **行星及以上**：通过 Cloudflare Worker 代理请求，稳定绕过 CORS

### 3.5 历史记录保存
- 游客：浏览器关闭即丢
- 卫星：本地永久，但换设备丢失
- 行星：云端 30 天
- 恒星：云端 90 天
- 星系：云端 1 年
- 宇宙：云端永久

---

## 四、到期与降级策略

1. **到期后 30 天宽限期**
   - 宽限期内：**只读模式**，不能新增数据
   - 可正常浏览、阅读、查看历史
   - 弹窗提示续费

2. **30 天后自动清理**
   - 超出免费额度（卫星 10MB）的数据自动删除
   - 删除顺序：从最旧的数据开始
   - 保留最新 10MB 数据

3. **续费后恢复**
   - 立即恢复全部功能
   - 已删除数据不可恢复

---

## 五、存储警告机制

| 使用率 | 行为 |
|--------|------|
| 70% | 静默提示（设置图标上的小圆点） |
| 85% | 弹窗提示"空间不足，建议清理或升级" |
| 95% | 强制弹窗 + 显示存储详情 |
| 100% | **禁止新增任何数据**，只能删除或升级 |

---

## 六、设备管理

### 6.1 设备数
- 游客：1（本机）
- 卫星：1
- 行星：10
- 恒星：20
- 星系：40
- 宇宙：无限

### 6.2 设备管理开关
- **默认关闭**
- 设置路径：我的 → 账号安全 → 设备管理

### 6.3 关闭状态（默认）
- 任何设备输入**账号+密码**即可登录
- 不验证设备身份
- 不限制设备数量（但受会员等级存储空间限制）

### 6.4 开启状态
- **新设备登录** → 需要**邮箱验证码**（后期支持手机号）
- **信任设备** → 免验证码，直接登录
- **设备上限** → 达到上限时，踢掉最久未使用的**非信任设备**
- **被踢通知** → 发送**邮箱通知**："您的账号在另一台设备登录，如非本人操作请修改密码"

### 6.5 信任设备
- 登录时可以选择"信任此设备"
- 信任后该设备免验证码
- 可随时在设备管理中取消信任
- 信任设备不受"踢掉最久未使用"规则影响

### 6.6 设备管理 UI
- "我的"页面添加"设备管理"入口
- 显示当前设备列表（名称、类型、最后活跃时间、地点）
- 可操作：设为信任、退出登录、移除设备
- 超限弹窗：选择踢掉哪台设备或升级会员

---

## 七、游客模式

- 不登录，打开即用
- 数据存在浏览器本地（IndexedDB / localStorage）
- 可以使用所有 AI 模型（用户自己填 API Key）
- 可以无限导入书源/图源
- 可以阅读小说/漫画
- **关闭浏览器/清除数据 = 全部丢失**
- 不能使用 Worker 代理
- 每次打开弹窗提示"登录后可云端同步，数据永不丢失"

---

## 八、头像右侧等级标签

```
游客:  [头像] 小宇宙                    ← 无标签
卫星:  [头像] 小宇宙 [卫星 🛰️]          ← 灰色小标签
行星:  [头像] 小宇宙 [行星 🪐]          ← 绿色小标签
恒星:  [头像] 小宇宙 [恒星 ☀️]          ← 蓝色小标签
星系:  [头像] 小宇宙 [星系 🌌]          ← 紫色小标签
宇宙:  [头像] 小宇宙 [宇宙 🌠]          ← 金色渐变标签
```

- 位置：侧边栏用户名称右侧
- 样式：圆角小标签，颜色对应等级
- 数据从 `profiles.plan` 或 `Store.state.membership` 读取

---

## 九、会员计划页面设计

### 9.1 页面结构
```
顶部：当前等级大卡片
      [宇宙 🌠] 宇宙会员
      已使用 45.2GB / 100GB (45%)
      [管理存储] [升级空间]

中间：等级对比（横向滑动或表格）
      游客 | 卫星 | 行星 | 恒星 | 星系 | 宇宙

底部：价格选择
      [月付] [年付] 切换
      价格卡片网格
      [立即订阅] 按钮
```

### 9.2 支付方式
- **当前仅展示价格，暂不接入实际支付**
- 页面显示"支付方式即将上线"或"联系管理员开通"
- 预留微信支付/支付宝/Stripe 接口位置

---

## 十、存储管理页面

### 10.1 "我的"页面存储卡片
```
┌─────────────────────────────────────┐
│  💾 存储空间                         │
│  [████████░░░░░░░░░░] 45%           │
│  已用 45.2GB / 100GB (宇宙)         │
│  [管理存储]  [升级空间]              │
└─────────────────────────────────────┘
```

### 10.2 存储详情页
```
存储详情
├── 📷 图片与头像      15.2MB  [清理]
├── 🎙️ 语音消息        8.1MB   [清理]
├── 📄 文档文件        5.5MB   [清理]
├── 💬 历史对话        12.3MB  [清理]
├── 📚 书源与书架      2.4MB   [清理]
├── 🎭 自定义角色      0.8MB   [清理]
└── ⚙️ 设置缓存        0.1MB   [清理]
```

---

## 十一、数据库设计（待实现）

### 11.1 设备表
```sql
CREATE TABLE user_devices (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  device_name text,
  device_type text,           -- mobile | tablet | desktop
  device_model text,
  os text,
  browser text,
  ip_address text,
  location text,
  last_active timestamptz,
  is_trusted boolean DEFAULT false,
  is_current boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);
```

### 11.2 会员等级配置表（已有）
```sql
-- membership_levels 表（在 AI-admin 的 schema.sql 中已定义）
-- level_key: guest | satellite | planet | star | galaxy | universe
```

### 11.3 用户资料扩展
```sql
-- profiles 表需扩展字段：
-- plan: text DEFAULT 'guest'          -- 当前会员等级
-- storage_used: bigint DEFAULT 0      -- 已用存储（字节）
-- storage_limit: bigint DEFAULT 0     -- 存储上限（字节）
-- device_limit: int DEFAULT 1         -- 设备上限
-- plan_expires_at: timestamptz        -- 会员到期时间
-- device_management: boolean DEFAULT false  -- 设备管理开关
```

---

## 十二、前端实现清单

### 需修改/新增的文件

| 文件 | 修改内容 |
|------|----------|
| `index.html` | 添加会员计划页面 DOM、设备管理页面 DOM、存储管理页面 DOM |
| `js/pages.js` | 添加 `renderMembership()`、`renderDeviceManagement()`、`renderStorageManagement()` |
| `js/store.js` | DEFAULTS 添加 `membership`、`devices`、`storage` 字段 |
| `js/supabase.js` | 添加设备表操作、存储统计同步 |
| `js/ui.js` | 侧边栏用户名称右侧添加等级标签 |
| `css/pages.css` | 会员页面、设备管理、存储管理样式 |
| `js/auth.js` | 登录时同步会员信息、设备记录 |

---

## 十三、与 AI-admin 的关系

- AI-admin 管理后台已有会员等级体系（`membership_levels` 表）
- AI-admin 已有订单、代理分润、审计日志等模块
- 本方案是**前端用户侧**的会员展示和限制逻辑
- 后端支付/订单逻辑在 AI-admin 中实现
- 当前阶段：前端先做展示和 Mock，支付接口预留

---

*本文档由 AI 维护，每次会员方案变更后更新。*


---

## 十四、超量处理策略（更新）

### 14.1 旧方案（已废弃）
~~到期30天后自动删除超出免费额度的旧数据~~

### 14.2 新方案：禁止新增 + 引导导出

**到期或超量后的处理流程：**

1. **达到 100% 存储上限**
   - 系统**禁止新增任何数据**（不能发消息、不能导入书源、不能上传图片）
   - 弹窗提示："存储空间已满，请导出数据或升级会员"

2. **导出数据界面**
   - 多选列表，用户自选要导出的内容：
     - [ ] 历史对话（可筛选时间段、特定对话）
     - [ ] 书源列表
     - [ ] 书架数据
     - [ ] 自定义角色
     - [ ] 聊天图片/文档
     - [ ] 语音消息
     - [ ] 设置偏好
   - 支持导出格式：JSON / Markdown / ZIP
   - 导出后询问："是否删除已导出的数据以释放空间？"
     - [删除并释放空间] / [保留数据]

3. **到期降级**
   - 30 天宽限期：只读模式，可以浏览、导出
   - 30 天后：仍然只读，不自动删除数据
   - 用户必须手动导出 + 删除，或续费恢复

4. **数据主权**
   - 用户的数据永远属于用户
   - 平台**永不自动删除**用户数据
   - 只通过"禁止新增"倒逼用户处理

---

## 十五、支付与激活

### 15.1 当前阶段：卡密系统

**暂不接入第三方支付，使用卡密激活。**

**卡密规则：**
- 卡密格式：`TP-XXXX-XXXX-XXXX`（16位字母数字）
- 卡密类型：
  - 行星月卡 / 行星年卡
  - 恒星月卡 / 恒星年卡
  - 星系月卡 / 星系年卡
  - 宇宙月卡 / 宇宙年卡
- 卡密由管理员在 AI-admin 后台生成
- 用户在前端「会员计划」页面输入卡密激活
- 卡密一次性使用，激活后绑定账号

**卡密激活流程：**
```
用户输入卡密 → 前端校验格式 → 调用 Supabase 函数验证
→ 卡密有效且未使用 → 更新 profiles.plan / plan_expires_at
→ 激活成功，刷新会员状态
→ 卡密标记为已使用，记录使用者和时间
```

### 15.2 预留支付接口

为未来接入第三方支付预留接口：

```javascript
// js/payment.js（预留文件）
const Payment = {
  // 微信支付
  async wechatPay(plan, duration) { /* 预留 */ },
  // 支付宝
  async alipay(plan, duration) { /* 预留 */ },
  // Stripe
  async stripePay(plan, duration) { /* 预留 */ },
  // 卡密（当前可用）
  async redeemCardKey(key) { /* 已实现 */ }
};
```

### 15.3 卡密数据库表

```sql
CREATE TABLE card_keys (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  key_code text UNIQUE NOT NULL,           -- 卡密字符串
  plan_type text NOT NULL,                  -- planet | star | galaxy | universe
  duration_months int NOT NULL,             -- 1 | 12
  is_used boolean DEFAULT false,
  used_by uuid REFERENCES auth.users(id),
  used_at timestamptz,
  created_by uuid,                          -- 管理员ID
  created_at timestamptz DEFAULT now(),
  note text                                 -- 备注（如"活动赠送"）
);

-- 卡密使用记录
CREATE TABLE card_key_logs (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  card_key_id uuid REFERENCES card_keys(id),
  user_id uuid REFERENCES auth.users(id),
  action text,                              -- 'redeem' | 'revoke'
  created_at timestamptz DEFAULT now()
);
```

---

## 十六、家庭共享

### 16.1 资格

**仅 ¥29.9 及以上的套餐支持家庭共享。**

| 等级 | 价格 | 家庭共享 |
|------|------|----------|
| 行星 ¥9.9 | ❌ 不支持 |
| 恒星 ¥29.9 | ✅ 支持 |
| 星系 ¥59.9 | ✅ 支持 |
| 宇宙 ¥99 | ✅ 支持 |

### 16.2 共享规则

- **共享人数**：主账号 + 最多 3 个子账号 = 共 4 人
- **共享内容**：
  - 存储空间：共用主账号的存储池（如恒星 10GB 全家共用）
  - 书源：共享书源列表
  - Worker 代理：共用
  - 云端同步：子账号享有云端同步
- **不共享内容**：
  - 历史对话：各自独立
  - 书架：各自独立（可选共享开关）
  - API Key：各自独立
- **子账号限制**：
  - 不能再次发起家庭共享
  - 不能单独升级会员（必须先退出家庭组）
  - 退出家庭组后，数据保留但降级为卫星（免费）

### 16.3 家庭组管理

```
家庭共享管理
├── 主账号：小宇宙 [恒星 ☀️]
│   存储：已用 6.2GB / 10GB
│
├── 成员列表
│   ├─ 👤 小宇宙（我） [主]
│   ├─ 👤 妈妈 [子]
│   ├─ 👤 爸爸 [子]
│   └─ 👤 弟弟 [子]
│
├── [邀请成员] （通过链接/二维码）
├── [解散家庭组]
└── [退出家庭组] （子账号显示）
```

---

## 十七、邀请有奖 — 抽奖系统

### 17.1 规则

- **触发条件**：邀请的用户**成为付费会员**（行星及以上）后，邀请人获得 1 次抽奖机会
- **邀请方式**：专属邀请链接 / 邀请码
- **抽奖次数**：可累积，不清零

### 17.2 奖品池

| 奖项 | 奖品 | 概率 | 说明 |
|------|------|------|------|
| 🥇 一等奖 | **宇宙会员 1 个月** | 0.01% | 几乎不可能，但存在 |
| 🥈 二等奖 | 星系会员 1 个月 | 0.1% | 极低概率 |
| 🥉 三等奖 | 恒星会员 1 个月 | 1% | 低概率 |
| 🎁 四等奖 | 行星会员 1 个月 | 5% | 中等概率 |
| 💾 五等奖 | 存储空间 2GB / 30 天 | 15% | 独立存储，无代理 |
| 💾 六等奖 | 存储空间 500MB / 7 天 | 30% | 独立存储，无代理 |
| 🕐 七等奖 | 存储空间 200MB / 3 天 | 35% | 独立存储，无代理 |
| 🙏 谢谢参与 | 无 | 13.89% | — |

> 总概率：100%（0.01 + 0.1 + 1 + 5 + 15 + 30 + 35 + 13.89 = 100）

### 17.3 独立存储说明

- 抽奖获得的存储空间是**独立于会员之外**的
- 只有存储空间，**没有 Worker 代理、没有云端同步、没有多设备**
- 可以理解为"临时扩容包"
- 有效期到期后自动失效，数据不删除但禁止新增
- 可与会员存储叠加使用

### 17.4 抽奖界面

```
🎰 幸运抽奖

您当前有 3 次抽奖机会

[🎲 开始抽奖]

奖品预览：
🥇 宇宙会员  🥈 星系会员  🥉 恒星会员
🎁 行星会员  💾 2GB/30天  💾 500MB/7天
💾 200MB/3天  🙏 谢谢参与

我的中奖记录：
2026-07-28  💾 500MB/7天
2026-07-25  🙏 谢谢参与
2026-07-20  💾 200MB/3天
```

### 17.5 数据库设计

```sql
-- 邀请记录
CREATE TABLE user_invites (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  inviter_id uuid REFERENCES auth.users(id),
  invitee_id uuid REFERENCES auth.users(id),
  invite_code text,
  invitee_paid boolean DEFAULT false,       -- 被邀请人是否付费
  lottery_earned boolean DEFAULT false,     -- 是否已获得抽奖机会
  created_at timestamptz DEFAULT now()
);

-- 抽奖记录
CREATE TABLE lottery_records (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES auth.users(id),
  prize_type text,                          -- 'membership' | 'storage'
  prize_detail jsonb,                       -- {plan:'universe', days:30} 或 {size:'500MB', days:7}
  prize_tier int,                           -- 1-7
  is_claimed boolean DEFAULT false,         -- 是否已领取
  claimed_at timestamptz,
  created_at timestamptz DEFAULT now()
);

-- 用户临时存储额度（抽奖获得）
CREATE TABLE user_bonus_storage (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES auth.users(id),
  size_bytes bigint,                        -- 奖励存储大小
  expires_at timestamptz,                   -- 过期时间
  is_active boolean DEFAULT true,
  created_at timestamptz DEFAULT now()
);
```

---

## 十八、更新后的完整权益表

| 权益 | 游客 | 卫星 | 行星¥9.9 | 恒星¥29.9 | 星系¥59.9 | 宇宙¥99 |
|------|------|------|----------|-----------|-----------|---------|
| 登录 | ❌ 免登录 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 存储空间 | 本地 | 10MB | 1GB | 10GB | 50GB | 100GB |
| 设备数 | 1 | 1 | 10 | 20 | 40 | 无限 |
| 云端同步 | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| Worker 代理 | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| 书源导入 | ✅ 无限 | ✅ 无限 | ✅ 无限 | ✅ 无限 | ✅ 无限 | ✅ 无限 |
| 历史保存 | 关闭即丢 | 本地永久 | 云端30天 | 云端90天 | 云端1年 | 云端永久 |
| 家庭共享 | ❌ | ❌ | ❌ | ✅ 4人 | ✅ 4人 | ✅ 4人 |
| 图片压缩 | 强制压缩 | 可选 | 可选 | 可选 | 可选 | 可选 |
| 设备管理 | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| 邀请抽奖 | ❌ | ❌ | 被邀请人付费后获得 | 同左 | 同左 | 同左 |
| 超量处理 | — | — | 禁止新增，引导导出 | 同左 | 同左 | 同左 |
| 到期处理 | — | — | 30天宽限期只读 | 同左 | 同左 | 同左 |

---

*本文档由 AI 维护，每次会员方案变更后更新。*
*最后更新: 2026-07-28 23:50*
